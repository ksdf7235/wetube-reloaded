extends ../base

block content
    if errorMessage 
        span=errorMessage
    form(method="post")
        input(placeholder="Old password", type="password",name="old")
        input(placeholder="New password", type="password",name="newpa")
        input(placeholder="New password Confirmation",type="password" ,name="confirm")
        input(value="Change Password" type="submit")